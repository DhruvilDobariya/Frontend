let $radioButton = document.querySelectorAll("input");

// we hvae different types of attribute like,

$radioButton[0].setAttribute("disabled", true); // set desable textbox
$radioButton[0].setAttribute("checked", true); // checked checkbox
